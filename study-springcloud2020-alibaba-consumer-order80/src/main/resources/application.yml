server:
  port: 80

spring:
  application:
    name: nacos-consumer
  cloud:
    nacos:
      discovery:
        server-addr: 192.168.1.13:8848

management:
  endpoints:
    web:
      exposure:
        include: '*'
